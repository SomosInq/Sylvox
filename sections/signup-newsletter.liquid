{% schema %}
{
  "name": "Newsletter",
  "tag": "section",
  "class": "newsletter-section",
  "settings": [
    {
      "type": "image_picker",
      "id": "background_image",
      "label": "Background Image"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Join Our Newsletter"
    },
    {
      "type": "textarea",
      "id": "subheading",
      "label": "Subheading",
      "default": "Be the first to know about product launches and all things inside Sylvox."
    },
    {
      "type": "image_picker",
      "id": "envelope_icon",
      "label": "Envelope Icon"
    },
    {
      "type": "text",
      "id": "email_placeholder",
      "label": "Email Placeholder",
      "default": "Email address"
    },
    {
      "type": "text",
      "id": "button_label",
      "label": "Button Label",
      "default": "Sign Up"
    },
    {
      "type": "image_picker",
      "id": "button_arrow_icon",
      "label": "Button Arrow Icon"
    },
    {
      "type": "image_picker",
      "id": "overlay_image",
      "label": "Bottom Right Overlay Image"
    }
  ],
  "presets": [
    {
      "name": "Newsletter",
      "category": "Somos Custom"
    }
  ]
}
{% endschema %}

{% style %}
.newsletter-section {
  background-image: url({{ section.settings.background_image | image_url }});
  background-size: cover;
  background-position: center;
  position: relative;
  min-height: 500px;
  display: flex;
  justify-content: center;
  align-items: center;
  padding: 20px;
  text-align: center;
  color: #1a1a1a;
}

.newsletter-content {
  display: flex;
  flex-direction: column;
  align-items: center;
  max-width: 700px;
  width: 100%;
}

.newsletter-section h2 {
  font-size: 2rem;
  margin-bottom: 10px;
}

.newsletter-section p {
  font-size: 1rem;
  margin-bottom: 30px;
  color: #555;
}

.newsletter-form {
  display: flex;
  align-items: center;
  background-color: #fff;
  padding: 10px;
  border-radius: 4px;
  width: 100%;
  max-width: 500px;
  box-shadow: 0 0 10px rgba(0,0,0,0.05);
}

.newsletter-form .icon {
  width: 20px;
  margin-right: 10px;
}

.newsletter-form input[type="email"] {
  border: none;
  flex-grow: 1;
  font-size: 1rem;
  padding: 5px;
}

.newsletter-form input:focus {
  outline: none;
}

.newsletter-form button {
  display: flex;
  align-items: center;
  background: none;
  border: none;
  color: #1a1a1a;
  font-size: 1rem;
  cursor: pointer;
}

.newsletter-form button img {
  width: 16px;
  margin-left: 5px;
}

.overlay-image {
  position: absolute;
  bottom: 0;
  right: 0;
  max-width: 100px;
  pointer-events: none;
}
{% endstyle %}

<div class="newsletter-section">
  <div class="newsletter-content">
    <h2>{{ section.settings.heading }}</h2>
    <p>{{ section.settings.subheading }}</p>
    <form method="post" action="/contact#contact_form" accept-charset="UTF-8" class="newsletter-form" id="contact_form">
      <input type="hidden" name="form_type" value="customer">
      <input type="hidden" name="utf8" value="✓">
      {% if section.settings.envelope_icon != blank %}
        <img class="icon" src="{{ section.settings.envelope_icon | image_url }}" alt="Email Icon">
      {% endif %}
      <input type="email" name="contact[email]" placeholder="{{ section.settings.email_placeholder }}" required>
      <button type="submit">
        {{ section.settings.button_label }}
        {% if section.settings.button_arrow_icon != blank %}
          <img src="{{ section.settings.button_arrow_icon | image_url }}" alt="Arrow">
        {% endif %}
      </button>
    </form>
  </div>
  {% if section.settings.overlay_image != blank %}
    <img class="overlay-image" src="{{ section.settings.overlay_image | image_url }}" alt="Overlay">
  {% endif %}
</div>
