{% stylesheet %}
<link rel="stylesheet" href="https://unpkg.com/flickity@2/dist/flickity.min.css">
<style>
  .product-tabs-section {
    transition: all 0.3s ease;
    padding: 2rem 1rem;
  }

  .product-tabs-section .tabs {
    display: flex;
    justify-content: center;
    gap: 16px;
    margin-bottom: 24px;
    flex-wrap: wrap;
  }

  .product-tabs-section .tab {
    padding: 8px 16px;
    border-radius: 9999px;
    cursor: pointer;
    background: #fff;
    border: 1px solid #ccc;
    transition: background 0.3s;
  }

  .product-tabs-section .tab.active {
    background: #20413B;
    color: white;
    border-color: #20413B;
  }

  .tab-content {
    display: none;
  }

  .tab-content.active {
    display: block;
  }

  .product-carousel {
    overflow: visible;
  }

  .carousel-cell {
    width: 250px;
    margin-right: 20px;
    opacity: 0.3;
    transform: scale(0.94);
    transition: all 0.3s ease;
  }

  .carousel-cell.is-selected {
    opacity: 1;
    transform: scale(1);
  }

  .product-card {
    background: white;
    padding: 10px;
    border-radius: 8px;
    text-align: center;
  }

  .product-card img {
    max-width: 100%;
    border-radius: 6px;
  }

  .product-card h3 {
    font-size: 1rem;
    margin-top: 10px;
  }

  .product-card p {
    font-size: 0.875rem;
    color: #666;
  }

  .carousel-arrows {
    display: flex;
    gap: 10px;
    justify-content: flex-end;
    margin-top: 20px;
  }

  .carousel-arrows button {
    width: 40px;
    height: 40px;
    border-radius: 50%;
    border: 1px solid #000;
    display: flex;
    align-items: center;
    justify-content: center;
    background: white;
    cursor: pointer;
  }

  .carousel-arrows button svg {
    width: 16px;
    height: 16px;
    fill: #333;
  }
</style>
{% endstylesheet %}

<section class="product-tabs-section section_width_{{ section.settings.section_width }}">
  <div class="section_content_width_{{ section.settings.section_content_width }}">
    {% if section.settings.heading %}
      <div class="header" style="text-align: {{ section.settings.header_alignment }};">
        <h2>{{ section.settings.heading }}</h2>
      </div>
    {% endif %}

    <div class="tabs">
      {% for block in section.blocks %}
        <div class="tab {% if forloop.first %}active{% endif %}" data-tab-id="tab-{{ forloop.index }}">{{ block.settings.tab_label }}</div>
      {% endfor %}
    </div>

    {% for block in section.blocks %}
      <div class="tab-content {% if forloop.first %}active{% endif %}" id="tab-{{ forloop.index }}">
        {% if block.settings.collection and block.settings.collection.products.size > 0 %}
          <div class="carousel-wrapper">
            <div class="product-carousel" id="carousel-{{ forloop.index }}">
              {% for product in block.settings.collection.products %}
                <div class="carousel-cell product-card">
                  <h3>{{ product.title }}</h3>
                  <p>{{ product.description | strip_html | truncate: 100 }}</p>
                  <img src="{{ product.featured_image | img_url: 'medium' }}" alt="{{ product.title }}">
                </div>
              {% endfor %}
            </div>

            {% if section.settings.show_arrows %}
              <div class="carousel-arrows">
                <button class="carousel-prev" data-carousel-id="carousel-{{ forloop.index }}">
                  <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24"><path d="M15 18l-6-6 6-6"/></svg>
                </button>
                <button class="carousel-next" data-carousel-id="carousel-{{ forloop.index }}">
                  <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24"><path d="M9 6l6 6-6 6"/></svg>
                </button>
              </div>
            {% endif %}
          </div>
        {% else %}
          <p style="text-align:center;">No products found in this collection.</p>
        {% endif %}
      </div>
    {% endfor %}
  </div>
</section>

{% javascript %}
<script src="https://unpkg.com/flickity@2/dist/flickity.pkgd.min.js"></script>
<script>
  document.addEventListener("DOMContentLoaded", function () {
    const tabs = document.querySelectorAll(".product-tabs-section .tab");
    const contents = document.querySelectorAll(".product-tabs-section .tab-content");

    tabs.forEach(tab => {
      tab.addEventListener("click", () => {
        const id = tab.dataset.tabId;

        tabs.forEach(t => t.classList.remove("active"));
        tab.classList.add("active");

        contents.forEach(c => {
          c.classList.remove("active");
          c.style.display = "none";
        });

        const activeContent = document.getElementById(id);
        if (activeContent) {
          activeContent.classList.add("active");
          activeContent.style.display = "block";
        }
      });
    });

    document.querySelectorAll('.product-carousel').forEach(carousel => {
      const flkty = new Flickity(carousel, {
        wrapAround: true,
        imagesLoaded: true,
        pageDots: true,
        prevNextButtons: false,
        cellAlign: 'left',
        contain: true
      });

      // Arrows
      const index = carousel.id.split("-").pop();
      const prevBtn = document.querySelector(`.carousel-prev[data-carousel-id="carousel-${index}"]`);
      const nextBtn = document.querySelector(`.carousel-next[data-carousel-id="carousel-${index}"]`);

      prevBtn?.addEventListener('click', () => flkty.previous());
      nextBtn?.addEventListener('click', () => flkty.next());
    });
  });
</script>
{% endjavascript %}

{% schema %}
{
  "name": "New Product Slider",
  "settings": [
    { "type": "text", "id": "heading", "label": "Heading", "default": "Bundles offer the best savings" },
    {
      "type": "select",
      "id": "header_alignment",
      "label": "Header Alignment",
      "default": "center",
      "options": [
        { "value": "left", "label": "Left" },
        { "value": "center", "label": "Center" },
        { "value": "right", "label": "Right" }
      ]
    },
    { "type": "checkbox", "id": "show_arrows", "label": "Show Arrows", "default": true },
    {
      "type": "select", "id": "section_width", "label": "Section Width", "default": "standard",
      "options": [
        { "value": "standard", "label": "50% Width" },
        { "value": "full", "label": "Full Width" }
      ]
    },
    {
      "type": "select", "id": "section_content_width", "label": "Content Width", "default": "standard",
      "options": [
        { "value": "standard", "label": "50% Width" },
        { "value": "full", "label": "Full Width" }
      ]
    }
  ],
  "blocks": [
    {
      "type": "tab",
      "name": "Tab",
      "settings": [
        { "type": "text", "id": "tab_label", "label": "Tab Label", "default": "Bundle Set" },
        { "type": "collection", "id": "collection", "label": "Collection" }
      ]
    }
  ],
  "presets": [
    {
      "name": "New Product Slider",
      "category": "Somos Custom",
      "blocks": [
        {
          "type": "tab",
          "settings": {
            "tab_label": "TV + Mount Bundles",
            "collection": ""
          }
        },
        {
          "type": "tab",
          "settings": {
            "tab_label": "TV + Soundbar Bundles",
            "collection": ""
          }
        }
      ]
    }
  ]
}
{% endschema %}
